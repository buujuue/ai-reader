# AI Reader 界面风格与 UI 设计准则

本文是 AI Reader 的项目级 UI 设计入口，用于统一页面、组件、布局、色彩、排版、图标、交互状态、响应式、动效与无障碍表现。新增、修改或评审任何用户可见界面前，必须先阅读本文。

本文总结自 `apps/reader/src/app/WorkbenchPrototype.tsx` 与 `apps/reader/src/app/workbenchPrototype.css` 的共识版 C 原型。原型负责提供视觉方向，不是产品范围、领域行为或生产代码结构的权威来源；发生冲突时，依次以 `CONTEXT.md`、`.scratch/reader-foundation/spec.md`、`docs/adr/` 和 `docs/architecture/overview.md` 为准。

## 一句话定位

**一个内容优先、专业克制、适合长时间使用的本地阅读工作台：外壳有低调的环境感，正文保持安静、稳定和清晰。**

它不是营销页、消费型内容流或炫技型 AI 面板。视觉上可以精致，但所有精致都必须服务于定位、组织、阅读和批注。

## 设计性格

- **内容优先**：阅读材料永远是视觉中心，工具只在需要时提高存在感。
- **专业而不冰冷**：继承桌面生产力工具的清晰结构，以低饱和蓝紫环境光和柔和材质降低机械感。
- **紧凑但不拥挤**：工作台外壳允许较高信息密度，正文区域必须留出呼吸空间。
- **沉浸但不隐藏语义**：减少视觉噪声，不以牺牲导航、状态、焦点或可发现性换取“极简”。
- **本地、可靠、可掌控**：状态反馈直接、克制、可理解，不使用夸张庆祝、诱导操作或不可预测的自动变化。

## 核心设计思路

### 1. 用工作台承载阅读，而不是把阅读器做成一张孤立页面

界面由稳定的工作台骨架组织：标题栏、活动栏、活动面板、编辑器组、阅读视图和状态栏。书库、目录、标签、批注入口与阅读设置围绕同一阅读材料协作，不把用户频繁送进互不相干的页面。

### 2. 外壳与正文使用不同的材质语言

- 标题栏、活动栏、活动面板、菜单和未来右侧栏属于“工作台外壳”，可使用半透明、细边框和适度背景模糊。
- 阅读视图属于“内容层”，使用稳定的不透明底色与清楚的正文对比，不叠加玻璃、强光晕或持续动画。
- 全窗只使用一张共享环境渐变；玻璃区域透出同一背景，不能让每张卡片各自生成渐变或发光。
- 阴影只表达真正的层级：窗口内框、浮层菜单、覆盖抽屉和纸张页。普通按钮和列表项主要用色阶与边框表达状态。

### 3. 功能区紧凑，阅读区舒展

- 外壳默认使用紧凑行高、短间距和小型线性图标，支持高频操作。
- 正文列保持受控宽度、较大内边距、约 `16px` 正文字号与 `1.8–1.95` 行高。
- 层级靠留白、字重、色阶和一条细边界建立，不靠堆叠卡片、彩色块或大标题建立。

### 4. 渐进披露，而不是把所有能力常驻在屏幕上

- 活动栏只保留最高频、稳定的区域切换；第一版主要是书库与目录。
- 低频全局操作进入“文件 / 编辑 / 视图”等菜单。
- 材料级操作进入阅读工具栏的材料操作菜单。
- 批注从材料操作菜单进入覆盖式材料批注面板，不占用未来 Agent 的右侧栏；正文高亮本身不承担打开批注面板的行为。
- “界面”活动面板与书库、目录共用左侧活动区域，用于承载工作台与阅读设置；同一活动面板同一时间只承载一种任务，侧栏可关闭、覆盖或调整宽度。

### 5. 状态优先于装饰

每个交互控件都应有可区分的默认、悬停、按下、活动、键盘焦点、禁用和进行中状态。活动状态使用“柔和强调底色 + 标记线、图标或文字变化”，不得只依赖颜色。

## 工作台结构

原型中的尺寸是视觉基准，不是绕过 `LayoutPolicy` 的硬编码许可：

| 区域 | 原型基准 | 设计意图 |
| --- | ---: | --- |
| 应用顶栏 | `46px` | 原生窗口装饰内的菜单、当前材料身份和工作台导航；系统窗口控制由平台提供 |
| 活动栏 | `54px` | 仅承载少量一级区域入口，图标与短标签并用 |
| 左活动面板 | `304px` | 默认可调，原型范围约 `244–460px` |
| 中央阅读区 | 弹性 | 始终获得剩余空间并保持最低视觉噪声 |
| 未来右侧栏 | `294px` | 仅作为后续 Agent 视觉参考，原型范围约 `230–470px` |
| 状态栏 | `25px` | 只显示低优先级环境与状态信息 |

生产界面必须遵守正式容器布局策略：

- **宽布局 `≥ 1200px`**：允许两个编辑器组，并可固定、调整两侧栏。
- **中布局 `800–1199px`**：允许两个编辑器组，但最多固定一个侧栏。
- **紧凑布局 `< 800px`**：只显示活动编辑器组，侧栏改为覆盖抽屉，隐藏状态不得被销毁。
- 布局依据工作台容器宽度计算，使用 `ResizeObserver` / 容器策略，不按 UA 或设备名称分支。
- iPadOS、Android 平板和带圆角/安全区的窗口必须处理 `safe-area-inset-*`；全高优先使用 `100dvh` 并提供回退。

### 区域约束

- 应用顶栏左侧放应用与导航，中间放当前材料身份；原生窗口控制保留给平台，应用顶栏的拖拽区域不得吞掉按钮点击。
- 活动栏图标保持少而稳定，不因新增命令无限增长；第一版入口为书库、目录和界面。
- 活动面板使用单一滚动容器；标题、搜索和摘要固定，内容区滚动；界面面板同样沿用宽度偏好和紧凑覆盖抽屉。
- 编辑器组的标签、活动视图和拆分状态必须可理解。共识版 C 原型隐藏标签行是视觉实验，生产界面在存在多标签或双组时不得隐藏必要导航。
- 阅读工具栏只保留源码模式和材料菜单等当前材料操作；排版从“界面”活动面板进入，左上角排版位置仅保留占位。
- 状态栏与轻量状态提示不得遮挡正文或抢占主要注意力。

## 色彩与材质

### 语义令牌

组件只能消费语义令牌，不直接散落十六进制色值。最小令牌集合如下：

- `app-bg`：全窗环境背景。
- `surface`：普通玻璃外壳。
- `surface-strong`：菜单、浮层和需要更高可读性的外壳。
- `surface-soft`：输入框、选中前的轻量容器。
- `surface-hover`：悬停与轻按反馈。
- `reader-bg`：阅读区外围背景。
- `page` / `page-text`：正文纸张与正文文字。
- `text` / `text-secondary` / `text-muted`：三级界面文字。
- `border` / `border-strong`：普通与强调边界。
- `accent` / `accent-strong` / `accent-soft`：活动、选择和结构强调。
- `focus`：键盘焦点，不能与普通活动态混用。
- `danger`：删除、关闭危险反馈，仅在语义需要时出现。
- `shadow`：浮层和主要容器阴影。

### 共识版 C 参考值

| 语义 | 暗色 | 亮色 |
| --- | --- | --- |
| `app-bg` | `#17181b` | `#e6e8eb` |
| `surface` | `rgb(21 23 28 / 59%)` | `rgb(247 248 251 / 38%)` |
| `surface-strong` | `rgb(18 20 24 / 86%)` | `rgb(247 248 251 / 68%)` |
| `reader-bg` | `#17191c` | `#e7e9ec` |
| `page` | `#1e2023` | `#f5f6f7` |
| `page-text` | `#d9dbde` | `#30343a` |
| `text` | `#f1f2f3` | `#20242a` |
| `text-secondary` | `#b7bac0` | `#515760` |
| `accent` | `#a2adbc` | `#5d6b7e` |
| `focus` | `#b9c5d6` | `#4e6380` |

暗色基准采用“玻璃暗度 4、环境光晕 1.4”的调校思路：只压暗工作台玻璃，不压暗正文；蓝、紫、青光晕只存在于全窗背景边缘，不用于文字发光、按钮描边或持续动画。亮色主题保持冷灰纸张与清晰边界，不能简单反转暗色。

### 对比度与回退

- 正文和普通界面文字至少满足 WCAG AA `4.5:1`；大字至少 `3:1`。
- `text-muted` 只能用于非关键辅助信息，不能承载唯一操作说明或关键状态。
- 背景模糊仅用于有限、稳定的外壳区域，不对滚动正文或大量列表项逐项使用。
- 不支持 `backdrop-filter`、处于高对比模式或性能不足时，回退为更不透明的 `surface-strong`，信息层级不能依赖模糊是否生效。

## 排版

### 界面字体

优先使用平台原生、中文清晰的无衬线字体：

```text
"Segoe UI Variable Text", "Segoe UI", "Microsoft YaHei", "PingFang SC", system-ui, sans-serif
```

- 工作台默认字号参考 `13px`，面板标题约 `15px / 650`，常规控件和输入不小于 `12px`。
- `10–11px` 只用于非关键 eyebrow、格式、计数和状态栏信息；不得用于主要按钮、正文或触控操作说明。
- 通过三级颜色、有限字重和间距建立层级，避免到处使用粗体。
- 标题截断使用单行省略并保留完整 `title` 或等价可访问名称。

### 阅读字体

- 中文长文默认回退为 `Source Han Serif SC`、`Noto Serif CJK SC`、`Songti SC`、`SimSun` 等衬线字体。
- 原型正文参考 `16px`、`1.95` 行高、受控列宽；标题使用约 `27–38px` 的响应式字号。
- UI 字体不得泄漏进 EPUB/PDF/Markdown 的格式语义。正文排版最终服从材料格式能力与用户阅读设置，本文只规定安全、清晰的默认回退。
- 长文中慎用两端对齐；必须确认中文、英文、窄屏与断词行为不会产生明显空洞。

## 间距、圆角与边界

- 采用以 `4px` 为基础的间距节奏，常用间距为 `4 / 8 / 12 / 16 / 24 / 32px`。
- 小控件圆角参考 `6–7px`，菜单与输入约 `8–11px`，主要工作台容器约 `15–16px`，胶囊与进度点使用 `999px`。
- 外壳边界通常为 `1px` 半透明线；不要用粗线把工作台切成表格。
- 同一层级只使用一种圆角尺度。避免每个内部元素都成为独立卡片。
- 正文纸张的留白显著大于工具区；窄屏可降低内边距，但不得让正文贴边。

## 图标

- 使用 Lucide 风格的一致线性 SVG 图标；尺寸通常为 `14–20px`，同一区域保持统一视觉重量。
- 不使用 emoji 充当功能图标，不混用填充、拟物、彩色插画和线性图标。
- 仅图标按钮必须有中文 `aria-label`，桌面端同时提供 `title` 或工具提示。
- 图标不能是状态的唯一载体；活动、错误、失联等状态同时提供结构、文字或形状提示。

## 交互与动效

- 通用过渡参考 `180ms cubic-bezier(0.2, 0.8, 0.2, 1)`；菜单进入可更短，约 `130–160ms ease-out`。
- 动效只解释出现、消失、选中、展开、拖动和空间连续性。禁止背景光晕漂移、滚动劫持、装饰性弹跳和大范围视差。
- 悬停通常改变文字色和轻量背景；按下可有轻微 `scale(0.96–0.98)`，但不得造成布局抖动。
- 展开/收起、菜单和抽屉必须支持 `Escape`、合理焦点进入、关闭后焦点归还。
- 尊重 `prefers-reduced-motion`；降低后仍应保留必要的状态变化，不得只把时间改成零而丢失反馈。
- 进行中操作应在 `100ms` 内给出反馈；成功或失败通过邻近状态、状态栏或 `aria-live` 轻量提示，不滥用模态框。

## 桌面、键盘与触控

- 所有功能必须可由键盘完成，Tab 顺序与视觉顺序一致；不得移除焦点环。
- 焦点样式至少为清晰的 `2px` 环，并与活动态可区分。
- 导航密集的工作台提供“跳到阅读正文”链接。
- 菜单使用菜单语义，树使用树语义，标签使用 tab 语义，调整手柄提供分隔条/滑块语义和方向键操作。
- 精确指针下，图标视觉框可以是 `28–34px`；触控或粗指针下，实际命中区域至少 `44×44px`，相邻目标至少留 `8px`，可通过伪元素扩大命中区。
- 不得把操作只放在悬停中。桌面端悬停显示的“更多”按钮必须可通过键盘聚焦；触控布局中应常驻或由明确入口替代。
- 拖拽只能作为快捷方式，必须提供键盘或菜单等价操作。

## 组件表现

### 应用顶栏与菜单

- 应用顶栏是安静的系统层，不使用品牌大字或装饰插画；第一阶段不替换平台原生窗口装饰。
- 工作台外壳默认采用 C 深色视觉，并提供原型确认的科技黑、简洁白、护眼橙、清新绿和甜蜜粉五套主题；可重排 EPUB 的正文基础纸张色与普通正文色跟随同一全局主题，固定版式 EPUB 与 PDF 保持原貌，未迁移格式继续使用自身阅读排版边界。
- 文本菜单适合高密度桌面操作；菜单项保持单行、清楚的快捷键列与分隔组。
- 首期菜单只暴露真实 Command：文件负责导入/备份/恢复/关闭当前材料，编辑负责书库筛选与当前材料搜索，视图负责书库/目录与阅读排版；编辑器拆分属于当前材料上下文，放入材料更多操作菜单；工作台主题与背景光归入“界面”活动面板，不迁移 Agent 或不可用占位动作。
- 菜单使用较强表面、细边框和有限阴影，避免多层嵌套玻璃。

### 活动栏与活动面板

- 活动栏入口使用图标 + 短标签，活动项增加柔和强调底和边缘标记。
- 书库面板默认使用真实、可持久化的文件夹树，空文件夹保持可见，已归类材料按唯一归属显示为文件夹叶子节点；未归类材料在文件夹树下方、回收站上方以独立可折叠区块展示，交互逻辑与回收站一致；单本材料通过“移动到……”菜单归类，标题/作者/文件夹筛选和封面按需加载继续保留，封面网格作为可选浏览方式。
- 搜索框是面板内的稳定入口，聚焦时提升边框与表面强度，不使用全屏搜索装饰。

### 阅读视图

- 阅读区背景比工作台外壳更稳定，正文页再高一个明度层级。
- 面包屑/位置、缩放、书签和材料菜单保持弱存在；阅读时不持续占用注意力。
- 材料更多菜单固定在阅读工具栏右侧，菜单内容保持材料级且精简；查看/导出批注、主要材料切换、编辑元数据和移入回收站不抢占正文，阅读排版统一从“界面”活动面板进入，视图菜单作为快捷聚焦入口，阅读工具栏左上角仅保留排版占位。
- 高亮使用与主题协调的半透明强调色，保留文字对比度。
- 高亮本身不是正文内的批注按钮；纯高亮与带文字笔记的批注在材料面板中明确区分，编辑入口只从面板提供。
- 阅读进度使用细轨道、百分比或页码；不使用大面积仪表盘。

### 材料批注面板

- 由材料操作菜单打开覆盖式面板，使用轻量强调边框、柔和底色和明确的关闭路径。
- 引文保持阅读字体，筛选、编辑、跳转和导出操作使用界面字体。
- 面板不固定占据右侧栏；右侧栏保留给未来 Agent，第一版不显示 Agent 占位。

### 空、加载、错误与失联状态

- 空状态简短说明“这里是什么、下一步能做什么”，不放无意义大插画。
- 加载优先保留布局尺寸，避免内容跳动；大列表和封面按需加载。
- 错误使用 `danger` 语义与可执行恢复建议，不只显示红色。
- 失联批注必须明确标记且保持可读，不伪装成普通成功状态。

## 原型中应继承与不应继承的内容

### 应继承

- 共识版 C 的“共享环境渐变 + 统一玻璃外壳 + 安静实体正文”材质关系。
- 类 VS Code 的稳定工作台骨架、互斥活动面板和材料级菜单分层。
- 树形层级、可调整侧栏、键盘焦点、跳转链接、实时状态提示和 reduced-motion 支持。
- UI 无衬线、正文衬线的职责分离，以及紧凑工具区 / 舒展阅读区的密度对比。
- 低饱和强调色、细边框、克制阴影与短时微交互。

### 不应继承

- `WorkbenchPrototype.tsx` 的模拟状态、组件边界、内联 mock 数据和绕过 Command/Repository 的原型行为。
- 第一版的 Agent 按钮、Agent 空面板或任何 AI 占位。原型右侧栏只用于验证长期构图；`docs/adr/0013-preserve-future-ai-boundaries-without-building-ai.md` 继续禁止第一版 AI 界面与空 Module。
- 共识版 C 中把编辑器标签行压成 `0` 高度的实验处理。正式标签可见性必须满足工作区行为与多标签导航。
- 原型的 `1180 / 900 / 760px` CSS 断点。生产布局使用架构规定的 `1200 / 800px` 容器策略。
- `9–10px` 关键操作文字、仅悬停可发现的触控操作或未校验对比度的 muted 文字。
- 为追求玻璃感而在正文、长列表的每一项或动画元素上叠加 `backdrop-filter`。

## Readest 参考与轻量化取舍

Readest 的对应实现主要位于：

- `C:\code\projects\readest\apps\readest-app\src\app\reader\components\ReaderContent.tsx`
- `C:\code\projects\readest\apps\readest-app\src\app\reader\components\sidebar\SideBar.tsx`
- `C:\code\projects\readest\apps\readest-app\src\app\reader\components\HeaderBar.tsx`
- `C:\code\projects\readest\apps\readest-app\src\hooks\usePanelResize.ts`
- `C:\code\projects\readest\apps\readest-app\src\styles\globals.css`

适合继承的成熟模式包括：固定/覆盖侧栏的响应式切换、可持久化面板宽度、鼠标/触摸/键盘调整、安全区处理、扩大触控命中区、窗口拖拽排除交互控件，以及 reduced-motion 回退。

AI Reader 不继承 Readest 的多服务工具栏、AI/TTS/翻译/同步入口、复杂多书网格、独立书库页面状态耦合和应用层历史包袱。视觉上以本原型的阅读工作台为准，底层阅读与跨端修复仍按根 `AGENTS.md` 要求优先检查 Readest。

## UI 任务执行顺序

1. 先确认任务没有越过产品范围、领域术语、ADR 与架构边界。
2. 检查 Readest 对应实现，说明适合继承的行为和准备舍弃的复杂度。
3. 读取本文，明确区域、密度、材质、令牌、状态与响应式策略。
4. 优先复用现有语义令牌、组件与 Command；不得在组件内另起行为通道。
5. 同时设计默认、悬停、按下、活动、焦点、禁用、加载、空、错误和窄屏状态。
6. 在桌面键盘、精确指针、平板触控、暗色、亮色、reduced-motion 与高对比回退下验证。

## 交付检查清单

- [ ] 阅读材料仍是视觉中心，新增工具没有抢占正文注意力。
- [ ] 只使用语义颜色、间距、圆角和动效令牌，没有组件级随意值。
- [ ] 暗色与亮色都达到文字对比要求，状态不只靠颜色表达。
- [ ] 所有操作可用键盘完成，焦点可见，弹层关闭后焦点归还。
- [ ] 触控命中区至少 `44×44px`，相邻目标有足够间距。
- [ ] 宽、中、紧凑三档按容器布局，隐藏面板或编辑器组不销毁状态。
- [ ] 安全区、`100dvh`、窗口拖拽区和平台窗口控制处理正确。
- [ ] 动效简短且有意义，支持 `prefers-reduced-motion`。
- [ ] 玻璃效果有不透明回退，未施加到正文和大量滚动项。
- [ ] 图标体系一致，无 emoji 功能图标，无只在悬停时可达的操作。
- [ ] 第一版没有因视觉占位引入 AI、Agent 或其它范围外能力。
- [ ] UI 行为仍通过稳定 Command 表达，视觉状态没有污染可序列化工作区或 Reader Runtime 边界。
