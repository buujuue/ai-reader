# AI Reader 领域上下文

AI Reader 是面向文史哲兴趣学习者的独立阅读产品。当前阶段先建立轻量、可靠、跨 PC 与平板的阅读工作区；AI 伴读能力属于后续阶段，不能反向污染第一版阅读内核。

## 当前正式术语

**独立产品**：从空仓库逐步构建、选择性借鉴 Readest 实现与架构的产品。它拥有自己的品牌、安装、数据格式和升级路径，不是 Readest 的发行版，也不承诺迁移 Readest 用户数据。
_Avoid_：Readest 定制版、Readest 升级版、Readest 精简分支

**阅读材料（Reading Material）**：已导入托管书库、可被打开阅读的 EPUB、PDF 或 Markdown 文档。阅读材料是领域实体，不等同于某次打开的界面实例。
_Avoid_：文件标签、阅读页面、工作区资源

**资料身份（Material Identity）**：由稳定 `BookId` 标识的一份阅读材料身份。`BookId` 在内容修改、元数据覆盖和应用升级中保持稳定。
_Avoid_：文件路径、标题作者组合、内容哈希

**完整内容指纹（Content Fingerprint）**：对托管文件全部字节计算的摘要，用于精确查重和完整性校验。它可以变化，不承担稳定身份职责。
_Avoid_：BookId、抽样 MD5、元数据哈希

**托管书库（Managed Library）**：由应用拥有并管理的私有文件空间。所有导入材料均复制进入托管书库；后续阅读、编辑、备份和删除只操作托管副本。
_Avoid_：外部文件夹镜像、原地阅读、用户原文件目录

**书库文件夹（Library Folder）**：托管书库内用于组织阅读材料的、可形成最多五层父子层级的应用内分类实体，不等同于操作系统文件夹。同一父级下的文件夹名称唯一，不同父级下可以同名。文件夹新建时必须立即命名；名称去除首尾空格、不能为空、最多 80 个字符，不能包含路径分隔符或控制字符，同级比较不区分大小写。每份阅读材料最多归属于一个具体书库文件夹，移动材料会改变其唯一归属而不会复制材料，首版一次只移动一份材料，移动可通过拖拽或“移动到”操作完成；文件夹可以改名，但创建后不改变父级。删除文件夹前需要确认。文件夹结构属于完整书库备份与恢复范围。
_Avoid_：文件系统目录、项目目录、标签、多文件夹副本

**未归类材料（Unfiled Material）**：尚未归属于具体书库文件夹的阅读材料。它不是书库文件夹；新导入的材料默认进入未归类，文件夹被删除时其中的材料也转为未归类而不是被删除。在书库面板中，未归类材料在文件夹树下方、回收站上方以独立可折叠区块展示。
_Avoid_：回收站材料、临时材料、默认文件夹

**书库面板（Library Panel）**：活动面板中的真实阅读材料入口。书库文件夹树是默认组织视图，已归类阅读材料作为文件夹树中的叶子节点；导入、标题/作者筛选、打开、元数据编辑和回收站行为都归属于同一入口。搜索同时匹配文件夹名称、材料标题和作者，并保留命中材料的层级路径；同级文件夹按名称、文件夹内材料按标题排序，空文件夹保留为可见节点。未归类材料在文件夹树下方、回收站上方以独立可折叠区块展示，并显示数量；封面网格保留为可选浏览视图，不改变材料与文件夹的领域关系。
_Avoid_：静态原型材料、独立书库页面、把操作系统目录当成书库结构

**来源元数据（Source Metadata）**：从阅读材料内部解析出的标题、作者、语言等不可编辑来源快照。
_Avoid_：当前显示元数据、用户整理结果

**元数据覆盖（Metadata Override）**：用户对标题、作者和封面的显式覆盖。有效元数据由覆盖值优先、来源值兜底计算。
_Avoid_：写回 EPUB、修改 PDF 属性

**阅读工作区（Reading Workspace）**：统一承载书库入口、标签、分栏、侧栏和阅读视图的应用工作台。整个书库只有一个工作区，不按书创建多个工作区。
_Avoid_：单书工作区、项目目录、窗口集合

**应用顶栏（Application Bar）**：原生窗口装饰内部的工作台导航层，第一阶段承载文件、编辑、视图三组真实菜单、当前材料身份和工作区导航；它不是自绘窗口控制条。
_Avoid_：自绘标题栏、Agent 占位菜单、只显示提示的假命令

**活动栏（Activity Rail）**：阅读工作区最左侧的窄图标区域，用于选择要展开的活动面板；它本身不是内容侧栏。
_Avoid_：活动面板、左侧栏

**活动面板（Activity Panel）**：由活动栏触发展开的左侧内容区域。活动面板在同一时间严格互斥，当前第一版包括书库和目录，后续左侧功能仍沿用这一单一区域。
_Avoid_：活动栏、多个并列左侧栏

**右侧栏（Right Sidebar）**：阅读工作区右侧独立预留给未来 Agent 交互的区域。第一版不在右侧栏承载批注或其它本地阅读工具，也不为了占位提前添加空壳 Agent 入口。
_Avoid_：批注栏、右侧批注面板、空壳 Agent 面板

**编辑器组（Editor Group）**：工作区中央承载多个标签的容器。第一版最多有两个编辑器组，可向右或向下拆分。
_Avoid_：阅读器实例、书籍网格

**阅读视图（ReadingView）**：某份阅读材料在某个编辑器组中的唯一打开实例。它拥有位置、导航历史、PDF 缩放和 Markdown 显示模式；再次从书库打开同一材料时激活已有视图，不创建重复视图。
_Avoid_：阅读材料、Foliate View、标签数据

**材料操作菜单（Material More Menu）**：附着在某个阅读视图或其阅读标签上的材料级操作入口，第一阶段位于阅读工具栏右侧。它承载查看/导出该材料批注、设置主要阅读材料、编辑材料元数据和移入回收站等动作；阅读排版仍使用阅读设置入口，它不是独立的书籍工作区。
_Avoid_：每本书工作区、全局更多菜单、批注活动栏

**主要阅读材料（Primary Material）**：由学习者显式指定、用于材料批注面板和未来书籍 Agent 归属的阅读材料。焦点切换不会自动改变它；只打开一份材料时自动将其设为主要阅读材料。
_Avoid_：当前焦点材料、当前标签

**BookDocument**：TS 阅读领域对 EPUB、PDF、Markdown 的统一文档 Interface，提供元数据、目录、章节/页面、导航、搜索、位置解析和封面等能力。`foliate-js` 与 PDF.js 是其主要 Implementation。
_Avoid_：原始文件、数据库记录、React 组件

**阅读位置（ReadingLocation）**：可序列化、可交给 BookDocument 恢复的视图位置。它不直接暴露具体渲染器对象。
_Avoid_：DOM Range、滚动像素、当前页 UI 文本

**批注（Annotation）**：用户创建的普通高亮、文字笔记或 PDF 区域标记，归属于阅读材料而不是某个阅读视图。
_Avoid_：未来 AI 批注、书签、搜索结果

高亮可以只有视觉标记而没有文字笔记；正文中的高亮不承担打开批注编辑器的点击入口，带笔记与否只在材料批注面板中区分。

**锚点（Anchor）**：将批注重新定位到材料内容的版本化数据。文本锚点组合 CFI、选中文字、前后文和文档版本；扫描 PDF 区域锚点组合页码与归一化坐标。
_Avoid_：仅 CFI、仅页码、DOM 节点引用

**失联批注（Orphaned Annotation）**：文档变更后无法唯一、安全恢复锚点的批注。它仍被保留并明确显示为失联，绝不静默附着到错误文本。
_Avoid_：已删除批注、自动猜测批注

**已重锚批注（Reanchored Annotation）**：文档版本变化后，依据唯一引文与上下文将锚点安全迁移到新 CFI 的批注。它明确显示为已重锚，
与原 CFI 仍可用的正常批注及无法安全恢复的失联批注区分。
_Avoid_：静默移动、未经唯一匹配的自动猜测

**工作区状态（Workspace State）**：可持久化的标签、组、活动视图、主要阅读材料、侧栏期望状态（包括书库文件夹树的展开状态）和导航历史；这些状态属于完整书库备份与恢复范围。
_Avoid_：Foliate 实例、选区、加载 Promise

**阅读运行时（Reader Runtime）**：不可持久化的 Foliate View、加载任务、当前选区、搜索结果和编辑会话等活对象集合。
_Avoid_：工作区状态、数据库仓储

**Command**：用户意图的稳定标识。按钮、菜单、键盘和触摸 Adapter 都执行同一个 Command。
_Avoid_：完成后的事件、直接操作 Store 的快捷键

**Event**：已经发生的事实通知，不表示待执行的用户意图。
_Avoid_：Command、跨模块随意消息

**模块（Module）**：隐藏实现细节并通过窄 Interface 提供能力的代码单元。
_Avoid_：泛指任意文件夹或 React 组件

**恢复快照（Recovery Snapshot）**：Markdown 未正式保存内容的自动持久化副本。它用于崩溃恢复，不等于正式文档版本。
_Avoid_：自动保存、备份包、文档历史版本

**显式版本迁移（Explicit Version Migration）**：用户确认后，把元数据匹配但完整内容指纹不同的 EPUB 更新到既有 `BookId`，并在一次可恢复提交中预览、迁移阅读进度与批注；它不是按标题、作者或元数据自动合并。
_Avoid_：自动合并、静默替换、近似版本猜测

**迁移恢复快照（Migration Recovery Snapshot）**：版本迁移提交前保存的本地 SQLite、旧 EPUB、批注和工作区一致副本。迁移后持续保留，只有用户明确清除才删除；崩溃恢复只回滚未完成操作。
_Avoid_：普通备份包、自动清理、只保存文件不保存状态

**完整书库备份（Full Library Backup）**：由 Rust 生成的版本化、未加密整库归档，包含 SQLite 一致快照、托管材料和两层封面；其中 SQLite 快照同时承载书库文件夹、材料唯一归属、工作区状态、阅读位置、批注和设置。恢复只支持整库替换，必须先在暂存区校验并为当前书库保留可回滚安全快照。
_Avoid_：仅导出书籍文件、应用层拼接的半份备份、合并两个书库

**备份格式版本（Backup Format Version）**：`manifest.json` 声明的归档协议版本。当前版本 v2 明确记录文件夹和材料归属；v1 旧归档仍可恢复，但没有文件夹数据，恢复后材料进入未归类且不恢复树展开状态。
_Avoid_：数据库 schema 版本、文档版本、静默猜测文件夹归属

## 后续阶段保留术语

以下概念继承自早期 AI 伴读构想，但全部不属于第一版阅读器范围。

**伴读**：学习者阅读材料的同时，围绕当前内容向 AI 提问并获得解释、背景或启发的连续过程，不是脱离材料的通用聊天。

**Agent 身份**：决定 AI 如何参与伴读的认知设定，包含阅读视角以及获准材料的引用和访问范围，但不拥有知识内容本身。

**Agent 模板**：可编辑、可新增、用于创建书籍 Agent 的身份预设。创建后的书籍 Agent 不随模板自动更新。

**书籍 Agent**：归属于一份阅读材料的独立 Agent 身份配置，不是全局助手，不建立隐藏长期记忆。

**伴读对话**：学习者与书籍 Agent 围绕阅读内容进行的一组连续问答。记录对用户完全可见；新对话不自动继承旧对话。

**趣味技能**：由学习者主动触发、通过工具白名单运行的声明式 AI 互动方式，不执行任意代码，也不参与默认问答。

**书籍索引**：书籍知识的第一披露层，包含书籍介绍与每章一句话概括，不包含章节全文。

**章节知识笔记**：书籍知识的第二披露层之一，由学习者与 Agent 共同维护并引用原书区域；它不是独立学习成果。

**原文章节片段**：书籍知识的原文层，由章节正文按稳定区域拆分而成。

**书籍知识包**：由书籍索引、可选章节知识笔记和原文章节片段组成的端到端加密同步对象，不包含原始电子书文件。

**章节上下文**：当前明确披露给书籍 Agent 的章节知识笔记与原文片段集合，独立于对话历史并对用户可见。

**AI 可见章节**：用户允许书籍 Agent 获得知识的章节集合。默认包括当前章节及以前章节，用户可手动调整。

**AI 笔记**：由书籍 Agent依据用户明确意图直接创建或整理、归属于阅读材料且本身也是 Markdown 阅读材料的文档。

**书籍笔记集**：归属于一份阅读材料的 AI 笔记集合，默认一份，只有用户明确需要时才扩展。

**AI 批注**：附着于原文的 AI 编写内容，必须与当前版本的普通高亮和用户笔记在数据类型上区分。

**Agent 知识库**：供 Agent 使用的多书引用视图，只保存规范引用与访问范围，不复制知识内容，也不是用户笔记库。

**分层取材**：Agent 先查看书籍索引，再显式读取章节知识笔记与原文章节片段；初始实现不依赖向量 RAG。

**AI 目录**：PDF 缺少自带目录时由 AI 依据页面文本整理的章节结构。第一版无 AI，因此不生成。

**同步后端**：在设备间中转端到端加密副本的可替换渠道，不拥有阅读领域规则，也不能读取明文。
