# `src/test/fixtures/pdf` — PDF 范围读取验收夹具

## 功能

- `pdfFixtures.ts`：确定性生成可由 PDF.js 打开的多页 PDF，并将第二页正文对象放在未引用的大体积尾部流之后，用于结构性检测文档信息、首屏和翻页阶段的全量读取回退。

## 依赖与边界

该目录只被真实浏览器性能验收脚本和测试使用，不参与生产运行时；生成器不承担 PDF 解析或渲染职责。
