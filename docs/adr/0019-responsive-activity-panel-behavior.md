---
status: accepted
---

# 活动面板按容器宽度响应式呈现

工作台继续由 `LayoutPolicy` 根据容器宽度决定活动面板的有效呈现：宽布局让活动栏、活动面板和阅读区并列；中布局最多固定一个侧栏；紧凑布局将活动面板改为覆盖抽屉。紧凑布局打开材料后自动收起抽屉，但只改变有效呈现，不清除面板期望状态、编辑器组或标签状态。

这样可以保留 C 原型的工作台层次，同时避免小窗口把正文压缩到不可读；代价是窄屏用户需要再次打开活动栏才能切换书库、目录或界面设置。
