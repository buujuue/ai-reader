---
status: accepted
---

# 面向 PC 与平板并采用单原生窗口

第一版完整目标为 Windows、macOS、iPadOS 和 Android 平板；不承诺手机、Linux 或完整 Web。Windows 是主要开发、讲解和完整验收平台，其他三端从第一天保持编译兼容并逐步增加原生冒烟验证。

第一版只有一个原生窗口。多任务通过工作区标签和最多两个 Editor Group 完成，避免把窗口生命周期、移动端限制和工作区状态同时复杂化。

布局按工作台容器实际宽度响应，不按 UA 或设备名称判断。平台差异只负责安全区、输入能力、系统权限和文件选择。
