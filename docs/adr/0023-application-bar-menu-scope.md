---
status: accepted
---

# 应用顶栏只承载首期真实菜单

原型迁移后的应用顶栏使用原生窗口装饰内的内容区域，首期只提供文件、编辑、视图三组真实菜单。文件包含导入材料、导出/恢复完整备份和关闭当前材料；编辑包含聚焦书库筛选和当前材料搜索；视图包含书库/目录切换和阅读排版入口。编辑器拆分属于当前阅读材料的上下文操作，统一放入材料更多操作菜单，并提供向右/向下两种拆分方向。每一项都通过稳定 Command 执行。

Agent、工作台壳主题切换、元数据维护以及没有生产 Command 支持的动作不进入顶栏，避免生成只能显示状态提示的假功能。
