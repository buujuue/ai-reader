---
status: accepted
---

# 在同步之前提供完整本地快照备份

第一版没有账户或云同步，但正式交付前必须提供完整本地备份。Rust 流式生成包含版本化 manifest、SQLite 一致快照、托管材料和封面的归档，不包含可再生成缓存或未正式保存的 Markdown 草稿。

第一版备份不加密，导出时必须明确提示其中包含书籍和私人批注。恢复先检查版本、指纹和空间，在暂存区完整验证，并在切换前为现有书库生成安全快照；失败后继续使用原书库。

第一版只支持整库恢复，不合并两套书库。单本批注可以导出为 Markdown，提供人类可读的数据出口。
