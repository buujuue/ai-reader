# 领域文档

本仓库采用单一领域上下文。

开始设计、拆票或实现前，依次阅读：

1. 根目录 `AGENTS.md`；
2. 根目录 `CONTEXT.md`；
3. 与任务相关的 `docs/adr/`；
4. `.scratch/reader-foundation/spec.md`；
5. 必要时阅读架构总览与产品愿景；
6. 检查 Readest 对应源码。

所有工单标题、验收标准和实现说明必须使用 `CONTEXT.md` 的正式术语。若与已接受 ADR 冲突，必须明确指出，不得静默覆盖。
